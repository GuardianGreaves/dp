<Window x:Class="diplom_loskutova.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:diplom_loskutova"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
        mc:Ignorable="d"
        BorderBrush="Black"
        BorderThickness="1"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Height="720"
        Width="1080"
        AllowsTransparency="True"
        Background="#fff">

    <DockPanel LastChildFill="True" Background="White">
        <Grid ClipToBounds="True" DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource ButtomMenu}" Content="Выход" Click="Button_Click" />
                <TextBlock x:Name="nameUser" VerticalAlignment="Center" FontSize="16" Margin="10 0 0 0" Grid.Row="0" Text="Иванов Иван Иванович"/>
                <TextBlock x:Name="nameRole" Foreground="Gray" VerticalAlignment="Center" FontSize="16" Margin="10 0 0 0" Grid.Row="0" Text="(ограниченный доступ)"/>
            </StackPanel>
            
            <!-- Меню -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
                <Button Style="{StaticResource ButtomMenu}" Content="Заявки" Click="ButtonOpenPageApplication"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Мероприятия" Click="ButtonOpenPageEvent"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Граждане" Click="ButtonOpenPageCitizen"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Пользователи" Click="ButtonOpenPageUsers"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Статус" Click="ButtonOpenPageStatus"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Тип мероприятия" Click="ButtonOpenPageTypeEvent"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Роль пользователя" Click="ButtonOpenPageRoleUser"/>
                <Button Style="{StaticResource ButtomMenu}" Content="Отчеты" Click="ButtonOpenPageReports"/> 
            </StackPanel>

            <!-- Контроль окна -->
            <StackPanel Margin="0 0 10 0" Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource ButtonWindowControl}" Width="30" Height="30" Click="Minimize_Click">
                    <iconPacks:PackIconMaterial Kind="WindowMinimize" />
                </Button>
                <Button Style="{StaticResource ButtonWindowControl}" Width="30" Height="30" Click="Restore_Click">
                    <iconPacks:PackIconMaterial Kind="WindowRestore" />
                </Button>
                <Button Style="{StaticResource ButtonWindowControl}" Width="30" Height="30" Click="Close_Click">
                    <iconPacks:PackIconMaterial Kind="Close" />
                </Button>
            </StackPanel>
        </Grid>

        <DockPanel Background="#f2f2f2">
            <Frame Grid.Row="1" x:Name="mainFrame" NavigationUIVisibility="Hidden" />
        </DockPanel>
    </DockPanel>
</Window>
