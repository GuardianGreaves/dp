<UserControl x:Class="diplom_loskutova.UserControlReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:diplom_loskutova"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Style="{StaticResource ButtomMenu}" Height="30" Grid.Row="0">
            <TextBlock x:Name="NameReport" FontSize="15" Text="Отчет"/>
        </Button>

        <StackPanel Grid.Row="1" Background="AntiqueWhite" Orientation="Horizontal">
            <Button Style="{StaticResource ButtomMenu}" Height="30" Click="Button_Click" 
                    Content="Word"/>
            <Button Style="{StaticResource ButtomMenu}" Height="30" Click="Button_Click_1" 
                    Content="Excel"/>
            <Button Style="{StaticResource ButtomMenu}" Height="30" Click="PrintButton_Click" 
                    Content="Печать"/>
        </StackPanel>

        <StackPanel x:Name="filter" Visibility="Collapsed" Grid.Row="2" Margin="5" 
                    Orientation="Horizontal">
            <DatePicker x:Name="filterDate" Width="150" 
                        SelectedDateChanged="filterDate_SelectedDateChanged"/>
        </StackPanel>

        <DataGrid x:Name="dataGridReport" Grid.Row="3"/>
    </Grid>
</UserControl>