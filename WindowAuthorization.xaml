<Window x:Class="diplom_loskutova.WindowAuthorization"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
        Width="710" Height="460" WindowStartupLocation="CenterScreen" 
        Background="Transparent" AllowsTransparency="True" WindowStyle="None">

    <Border Background="Transparent" MouseDown="Border_MouseDown" BorderBrush="Gainsboro" BorderThickness="1">
        <Grid Background="#f2f2f2">
            <Frame x:Name="mainFrame" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button x:Name="CloseButton" Margin="5" Background="Transparent" HorizontalAlignment="Right" 
                VerticalAlignment="Top" BorderThickness="0" Foreground="Gray" Height="24" Width="24" 
                Click="Button_Click">
                <iconPacks:PackIconMaterial Kind="Close"/>
            </Button>
        </Grid>
    </Border>
</Window>
