<Page x:Class="diplom_loskutova.Page.Applications"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ScottPlot="http://schemas.scottplot.net/2008/xaml/presentation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Application">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Button Style="{StaticResource ButtomMenu}" Height="30" Grid.Row="0" HorizontalAlignment="Left">
            <TextBlock FontSize="15" Text="Заявки "/>
        </Button>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource ButtomMenu}" Height="30" Grid.Row="0" HorizontalAlignment="Right" x:Name="btnDelete" Click="BtnDelete">
                <TextBlock FontSize="15" Text="Удалить"/>
            </Button>
            <Button Style="{StaticResource ButtomMenu}" Height="30" Grid.Row="0" HorizontalAlignment="Right" x:Name="btnAdd" Click="BtnAdd">
                <TextBlock FontSize="15" Text="Добавить"/>
            </Button>
            <Button Style="{StaticResource ButtomMenu}" Height="30" Grid.Row="0" HorizontalAlignment="Right" x:Name="btnChange" Click="BtnChange">
                <TextBlock FontSize="15" Text="Редактировать"/>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <!-- Всего пользователей -->
            <Border Grid.Column="0" Style="{StaticResource RoundedBorderStyle}" Background="LightBlue">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Всего: " FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock x:Name="tbTotalUsers" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
                </StackPanel>
            </Border>

        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="listViewApplication" MouseDoubleClick="ListViewStatus_MouseDoubleClick" Grid.Column="0" ItemsSource="{Binding}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding [ID_Заявки]}"/>
                        <GridViewColumn Header="Гражданин" DisplayMemberBinding="{Binding [ИмяПользователя]}"/>
                        <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding [СтатусМероприятия]}"/>
                        <GridViewColumn Header="Мероприятие" DisplayMemberBinding="{Binding [Название]}"/>
                        <GridViewColumn Header="Дата создания" DisplayMemberBinding="{Binding [Дата_Создания]}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Background="Ivory">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Поиск по ФИО"/>
                        <ComboBox Grid.Row="1" x:Name="ComboBoxSearchFIO" SelectionChanged="ComboBoxSearchFIO_SelectionChanged"/>

                        <TextBlock Grid.Row="2" Text="Поиск по типу статусу"/>
                        <ComboBox Grid.Row="3" x:Name="ComboBoxSearchStatus" SelectionChanged="ComboBoxSearchStatus_SelectionChanged"/>

                        <TextBlock Grid.Row="4" Text="Поиск по мероприятию"/>
                        <ComboBox Grid.Row="5" x:Name="ComboBoxSearchEvent" SelectionChanged="ComboBoxSearchEvent_SelectionChanged"/>

                        <TextBlock Grid.Row="6" Text="Поиск по дате"/>
                        <DatePicker Grid.Row="7" x:Name="DatePickerSearch" SelectedDateChanged="DatePickerSearch_SelectedDateChanged"/>

                        <Button Margin="0 10 0 0" Grid.Row="10" Content="Сбросить фильтр" Click="BtnResetFilter_Click" />
                    </Grid>
                </StackPanel>
                <TextBlock  Grid.Row="1" Text="Количество заявок на каждый статус " TextAlignment="Center" FontSize="14"/>
                <ScottPlot:WpfPlot x:Name="WpfPlot1" Grid.Row="2"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
